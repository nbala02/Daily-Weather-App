import React from 'react'
import ReactDOM from 'react-dom'
import './index.css'
import App from "./App"

// Use React DOM to render the weather app and get the element by the id "root"
ReactDOM.render(<App />, document.getElementById('root'))